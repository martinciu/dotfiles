# Catppuccin Mocha theme. Selection colours intentionally omitted — Mocha's
# defaults are well-tuned. Revisit if selections read poorly in practice.
# Active when ~/.config/ghostty/theme.ghostty → this file (theme-set mocha).
theme = Catppuccin Mocha
